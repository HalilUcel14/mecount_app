import 'package:flutter/cupertino.dart';

class HomeViewDesktopWeb extends StatelessWidget {
  const HomeViewDesktopWeb({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('isWeb'),
    );
  }
}
